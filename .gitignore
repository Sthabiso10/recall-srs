node_modules/
dist/
out/
build/
coverage/
.next/
.turbo/

# tsc --noEmit with `incremental` (Next.js sets it) drops these everywhere.
*.tsbuildinfo

.DS_Store
.env
.env.local
.env.*.local

# Wrangler's local dev state — machine-local, not source.
.wrangler/

# Editor/agent tooling config, like .vscode. Local to whoever is working here.
.claude/
